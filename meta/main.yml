---
galaxy_info:
  author: Jacopo Secchiero
  description: Playbook for postgres-slave-init.
  company: Enter srl
  license: MIT
  min_ansible_version: 2.4
  platforms:
    - name: Ubuntu
      versions:
        - xenial
  galaxy_tags:
    - molecule

dependencies:
  - {role: jsecchiero.postgresql-slave-init, version: 1.0.2}
  # List your role dependencies here, one per line.

